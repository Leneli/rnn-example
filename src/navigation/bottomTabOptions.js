import { COLORS } from '../constants/colors';

export const bottomTabOptions = {
  textColor: COLORS.dark,
  iconColor: COLORS.dark,
  selectedTextColor: COLORS.electricViolet,
  selectedIconColor: COLORS.electricViolet,
};
